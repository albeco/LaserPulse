This folder contains unit test files for the LaserPulse Matlab class.

To run all the tests use the command 'runtests'.

To run a specific test use the command 'run'.
For instance:
'results = table(run(checkUnit_test))'
will run the tests contained in the file 'checkUnit_test.m'.
